// Shared runtime state of the settings page (server-provided field options, labels, section titles, last fetched configuration and device list).
export const state = {
    fieldOptions: {},
    fieldLabels: {},
    sectionTitles: {},
    lastConfig: undefined,
    devices: undefined
};
